// Single source of truth for the studio's display name + logo.
import logo from '../assets/logo.png'

export const STUDIO_NAME = "I'm Possible Fit"

// The studio logo (imported so Vite fingerprints + optimizes it). Reference this
// everywhere a logo is shown so it stays consistent across the app.
export const LOGO_SRC = logo
